# Проверка обновления Frontier

Среда: Blender 4.5.3 LTS, Godot 4.7 stable, Apple M1 Pro / Forward+ (Metal).

- 27 GLB экспортированы из Blender; у восьми персонажей есть skin и шесть анимаций.
- `tests/regression.tscn`: 13 проверок, 0 неуспешных.
- `tests/frontier_expansion.tscn`: 34 проверки, 0 неуспешных.
- Основная сцена: 24 000 кадров с фиксированным шагом 1/60 секунды, то есть 400 секунд симуляции; выход 0, ошибок игровых скриптов в журнале нет.
- Оконный запуск Forward+: проверены дневной и ночной кадры и постановочная сцена зданий; ошибок движка в журнале оконной проверки нет.
- В headless-режиме macOS сообщает об отсутствии доступа к системным CA-сертификатам. Сетевая функциональность в этих проверках не используется. Звуковые данные PCM проверены непосредственно; воспроизведение выключено в автоматических тестах, поскольку Dummy-аудиодрайвер оставляет незавершённые playback-объекты при мгновенном завершении теста.

Автоматические тесты покрывают боевую смерть, однократные трофеи, население, туман войны, загрузку моделей/анимаций, переключение инструментов, вместимость рабочего, броню, стрелы скелета, ворота, независимые улучшения шахт, эвакуацию шахтёров, предупреждение и радиус взрыва, обезвреживание крипера, урон стенам, состав третьей волны и пути от всех алтарей до базы.

Это проверка работоспособности и стартовой настройки баланса, а не замена длительному ручному прохождению кампании.

## Исправление ночных волн

`tests/night_raid.tscn` с `--fixed-fps 60`: 11 проверок, 0 ошибок. Проверены автоматический запуск в 19:30, перемещение скрытых монстров, появление у поселения, фактический урон защитникам (после изменения приоритета целей), завершение волны, запуск после скачка времени на 23:30, отсутствие повторного запуска и движение с физикой от всех четырёх алтарей. Длительность сценария — 120 секунд симуляции.

В RaidSystem убрана верхняя граница вечернего окна 22:48, из-за которой могла пропускаться волна. HUD теперь постоянно показывает ожидание/остаток монстров/отбитую волну; миникарта отмечает активные алтари, сохраняя туман для самих врагов.

## Колония и подземный слой

Новые Blender-исходники и GLB: казарма (замена), ферма, колодец, олень, кабан, кролик. Посевы экспортируются отдельным мешем CropPlants. Рендер каталога: `art/colony-gallery.png`.

- `tests/colony_systems.tscn`: 26 проверок — материалы тумана, память разведки, ближайшая цель, смена здания на юнита, разделение слоёв, строительство, зависимость урожая от колодца, рост модели посевов, охота и добыча, вход/выход, копание и обновление A*, подземный спавн и задержка возрождения, отмена прежнего приказа, подсказки наведения.
- `tests/navigation.tscn` с `--fixed-fps 60`: 6 проверок с настоящей физикой — выбор обхода высокой скалы, прыжок на один блок, прохождение двух низких ступеней, встречные группы, очередь шести рабочих в проход шириной одну клетку, доставка рабочего через ворота и завершение казармы.
- Повторяются `regression` (13), `frontier_expansion` (34), `night_raid` (11). Полный набор — 90 проверок; все пройдены, 0 неуспешных.
- `tests/colony_preview.tscn`: оконная проверка поверхности и подземного разреза с миникартой, ошибок в журнале оконного запуска нет; сохраняет `art/colony-surface.png` и `art/colony-underground.png`.

Для headless-запусков указывайте `--log-file /tmp/<test-name>.log`: среда исполнения ограничивает запись в пользовательский каталог Godot. Ошибка доступа к системным CA-сертификатам в macOS не относится к игровым проверкам. Импорт моделей в headless-редакторе дополнительно сообщает о невозможности сохранить внешние настройки редактора; импорт самих ресурсов завершается.

## Архипелаг и фракция гоблинов — 14 сентября 2026

Итог: **154 проверки, 0 неуспешных**, ошибок игровых скриптов в итоговых журналах нет. Журналы сохранены в `art/test-results/`.

| Сценарий | Проверок |
| --- | ---: |
| regression | 13 |
| frontier_expansion | 34 |
| colony_systems | 26 |
| navigation | 6 |
| night_raid | 11 |
| archipelago | 45 |
| naval_journey | 10 |
| archipelago_simulation | 9 |

`archipelago` проверяет шесть островов, сокровища, разделение сухопутных маршрутов, физическую добычу и сдачу мяса, безопасное восстановление прерванной доставки, исчезновение общей цели-ресурса, очередь/время/возврат затрат/резерв населения/флаг, независимую экономику бота, ограничения порта, воду для спуска, посадку/высадку, морской маршрут, волков/медведей, строительство ботом, отдельный ночной рейд и видимость чужих зданий.

`archipelago_simulation`: 25 200 физических кадров при шаге 1/60 секунды, 420 секунд мира. Бот сам завершил хижины, казарму, шахту, колодец, ферму, башню, склад и порт, обучил рабочих и все пять боевых ролей, построил лодку и пережил ночной рейд. Рабочие фактически сдали **430 древесины**. Проверка учитывает доставку, а не остаток кошелька после расходов.

`naval_journey`: галера физически проплыла от домашнего острова к острову гоблинов с тремя пассажирами и автоматически высадила их на разные свободные клетки. Это проверка движения и столкновений, а не только наличия вычисленного пути.

После визуальной проверки исправлено самоперекрытие чужих зданий в тумане. `archipelago` и `colony_systems` повторно прошли после этой правки; оконный Forward+/Metal запуск без ошибок сохраняет кадры порта и поселения. Длительная симуляция проверяет развитие экономики; отдельные сценарии проверяют последнюю правку видимости.

24 новых GLB созданы через MCP в открытом Blender. Восемь новых персонажей имеют skin и шесть анимаций; названия клипов дополнительно проверены в GLB. Экспорты повторно импортированы в Blender и Godot; пустая резервная сцена исключена из GLB. Проверены каталоги `archipelago-units.png`, `archipelago-buildings.png`; рендеры созданы из игровых экспортов, исходная пользовательская сцена сохранена отдельно.

`art/archipelago-map.png` построен из фактического `island.json` и показывает расположение шести островов без тумана. Каталоги и постановочные игровые кадры служат визуальной проверкой; длительное ручное прохождение и баланс поздних волн остаются предметом дальнейшего тестирования.


## Навигация, активная фракция, стражи и оптимизация — 15 сентября 2026

Итоговый набор: **215 проверок, 0 неуспешных**. Журналы текущих сценариев находятся в `art/test-results/`; предыдущие разделы выше описывают исторические результаты прежних обновлений.

| Сценарий | Проверок |
| --- | ---: |
| regression | 13 |
| frontier_expansion | 34 |
| colony_systems | 26 |
| navigation | 8 |
| night_raid | 11 |
| archipelago | 45 |
| naval_journey | 10 |
| archipelago_simulation | 9 |
| ai_campaign | 20 |
| raid_growth | 13 |
| navigation_jobs | 5 |
| rts_optimization | 21 |

Навигационные сценарии проверяют встречные группы разных размеров (рабочий, гоблин, скелет, паук, медведь, тролль), шесть юнитов в проходе шириной одну клетку, встречный проход, уступание места стоящим союзником и строительство за воротами. Отдельно восемь рабочих физически собирают ресурсы и сдают полный груз; проверены обход новой стены, восстановление доставки после потери пути и отсутствие телепортации удалённого шахтёра.

`ai_campaign` проверяет настоящий цикл экспедиции: подготовка отряда, погрузка, плавание, высадка на домашнем острове, местная разведка, атака защитников, завершение аванпоста и возвращение галеры. Пара бойцов физически входит в пещеру. В этом сценарии флот, дополнительные бойцы и ресурсы выдаются тестом для изоляции логики миссии; темп самостоятельного развития проверяется отдельно.

`archipelago_simulation`: 420 секунд симуляции, 25 200 физических кадров. Бот без тестового пополнения кошелька завершает колодец, фермы, шахту, склад, казарму, башни, хижины и порт. Порт отмечен готовым к 150-й секунде, все пять боевых ролей обучены к 240-й, галера появляется к 270-й. Доставлено **1038 древесины** (перед оптимизацией алгоритмов — 1046, в предыдущей версии бота — 430). Поселение переживает ночь. Это результаты конкретного сценария; сроки не гарантируются при вмешательстве игрока.

`raid_growth` проверяет рост численности и характеристик пяти волн, а также волну на 124 противника: одновременно 64 живых, остальные в очереди; погибший освобождает место подкреплению, HUD считает всех ожидающих. `ai_campaign` дополнительно проверяет профили двух стражей, предупреждение площади удара, уклонение, попадание и задержку возрождения.

`rts_optimization`: 21 проверка. На 1024 лёгких тестовых акторах множество соседей совпадает с полным перебором: 7056 кандидатов вместо 1 048 576. Для 50 неподвижных наблюдателей пять обновлений дают ту же видимость при 9850 проверках лучей вместо 49 250. Проверены память тумана, изменения препятствий, живые блокировки кэшированного маршрута, точная конечная позиция, освобождённые акторы и перемещение из гарнизона. Это микротест алгоритмов, не обещание FPS с 1024 анимированными боевыми юнитами. Точные времена и границы внедрённой архитектуры: [RTS_ARCHITECTURE.md](RTS_ARCHITECTURE.md).

После последней правки индекса при высадке/выходе из шахты/переходе в пещеру повторены `rts_optimization`, `colony_systems`, `naval_journey` и `ai_campaign`; все прошли. Остальные журналы относятся к той же оптимизации перед этими точечными уведомлениями индекса.

Новые `ancient_golem.glb` и `storm_warden.glb`: по одному skin и шесть клипов Attack/Build/Gather/Idle/Run/Walk, экспортированных из Blender. Каталог `art/guardians.png` и кадр `art/guardian-gameplay.png` просмотрены. После перехода тумана на R8 выполнен оконный запуск Godot Forward+/Metal, ошибок в `guardian_preview.log` нет.

Во всех итоговых игровых журналах отсутствуют ошибки скриптов и предупреждения выполнения. В headless-журналах остаётся сообщение macOS о системных CA-сертификатах, не связанное с игровой симуляцией. Результаты не заменяют длительное ручное прохождение и профилирование поздней кампании с крупными армиями.


## HPA, общие поля и GPU-анимация — завершение 15 сентября 2026

Итоговый набор: **245 проверок, 0 неуспешных** в 17 сценариях. Текущие журналы заменены в `art/test-results/`. Предыдущие разделы сохраняют результаты предыдущих этапов.

К 215 проверкам предыдущего этапа добавлены:

| Сценарий | Проверок | Что проверено |
| --- | ---: | --- |
| hierarchical_flow | 15 | Реальные порталы и компоненты, точная цель, перестроение одного кластера, закрытие/открытие прохода, одно поле для 49 разных стартов, отмена приказа, физическое достижение строя, версия карты |
| crowd_render | 8 | Полноценные скелеты вместе с GPU-анимацией, остановка лишних AnimationPlayer, одна поверхность, выделение и наведение, скрытие туманом и посадка, освобождение пустых групп |
| physics_backend_profile | 2 | Сравнение траекторий 200/1000 тел между узлами и прямым PhysicsServer |
| rts_render_integration | 4 | Массовый рендер в Main, физическое движение, выделение, исключение скрытых врагов |
| navigation_bounds | 1 | Безопасная обработка отсутствующих клеток и координат вне карты |

`hierarchical_flow` проводит настоящие CharacterBody3D через лабиринт, а не только проверяет список точек: 49 юнитов заняли разные места в строю к 140-й секунде; пятидесятый остался на месте после отмены приказа. По результатам этой проверки исправлены потеря исходной цели при повторном уступании дороги и взаимное ожидание занятых мест. Проверки ворот, встречного движения, строительства и доставки ресурсов повторены после этих исправлений.

После изменения навигации повторно пройдены `ai_campaign` (20) и `archipelago_simulation` (9). В последнем прогоне за 420 секунд бот доставил **1088 древесины**, построил хозяйственные и военные здания, порт, обучил все боевые роли и создал галеру. Порт отмечен к 150-й секунде, все боевые роли и галера — к 240-й. Поселение пережило ночной рейд. Выдача ресурсов тестом не используется; темп относится к этому сценарию, а не гарантированному темпу любой партии.

`regression`, `frontier_expansion`, `naval_journey` и `raid_growth` также повторены на новой навигации. `night_raid`, `colony_systems`, `archipelago` и `rts_optimization` прошли с HPA и новой системой представления до последней локальной правки уступания дороги. Последняя защита границ координат дополнительно проверена отдельным сценарием `navigation_bounds`.

Оконный запуск Main на Forward+/Metal прошёл без ошибок: [кадр общего обзора](rts-overview.png). Проверено переключение скелета при выделении; туман скрывает чужих акторов, а батчинг не останавливает движение. Сравнение обычных и запечённых моделей — [crowd-comparison.png](crowd-comparison.png).

Отдельные профили 50/200/1000 маршрутов и моделей сохранены в JSON и описаны в [RTS_ARCHITECTURE.md](RTS_ARCHITECTURE.md). В изолированном тесте 1000 анимированных моделей медианное время кадра снизилось с 35,137 до 9,460 мс. Это не нагрузка полной боевой кампании. Прямой PhysicsServer не дал значимого выигрыша медианы, поэтому игровой backend CharacterBody3D сохранён.

Для повторения запускайте сцены `tests/<scenario>.tscn` из корня проекта с Godot 4.7. Навигационные сценарии требуют `--fixed-fps 60`; рендерные сценарии запускаются в оконном режиме с Metal, остальные допускают `--headless`. Профили: `tests/path_profile.tscn`, `tests/crowd_render.tscn`, `tests/physics_backend_profile.tscn`. Обязательно задавайте доступный `--log-file`.

В итоговых журналах нет ошибок игровых скриптов; headless macOS по-прежнему выдаёт сообщение доступа к системным CA-сертификатам. Длительное ручное прохождение и общий профиль поздней кампании остаются отдельной проверкой.


## Навигация, интерфейс и полная сцена — 16 сентября 2026

**255 успешных проверок в 16 сценариях; 0 неуспешных в итоговых журналах** [текущего прогона](test-results/2026-09-16/). Сценарии: regression (13), frontier_expansion (34), colony_systems (26), archipelago (45), naval_journey (10), ai_campaign (20), raid_growth (13), archipelago_simulation (9), navigation (8), navigation_jobs (5), live_navigation (5), night_raid (11), hierarchical_flow (15), rts_optimization (21), hud_controls (12), rts_render_integration (8). Это набор данного этапа; старые профили физического backend и отдельный crowd-render микротест повторно не запускались.

- `live_navigation`: проверены центр и западный край настоящего костра, 12 одновременных спавнов без наложения, 12 рабочих в реальном лесу и группа воинов против зомби. К 70-й секунде рабочие доставили 598 древесины.
- `navigation` и `navigation_jobs`: уступание дороги, встречные группы, узкие ворота, стройка, полный цикл доставки, новая стена и потерянный маршрут. После последних изменений запущены повторно вместе с live_navigation и HUD.
- `hierarchical_flow`: 49 из 49 акторов заняли свои места к 130-й секунде; отменённый пятидесятый приказ не возобновился. Возврат после уступания дороги теперь не требует предварительного освобождения всей дороги союзниками.
- `night_raid`: автоматическое появление волны, движение в тумане, атака защитников, отражение первого нападения и физический проход от всех четырёх алтарей. В подходах к цели учтена погрешность остановки 0,16 клетки: точка назначения должна лежать уверенно внутри радиуса взаимодействия.
- `archipelago_simulation`: за 420 секунд самостоятельного развития доставлено 826 древесины; хозяйство, военные роли, порт и галера созданы, поселение пережило ночь. `ai_campaign` отдельно проверил высадку, разведку, атаку игрока, аванпост и возвращение корабля. Эти два прогона выполнены перед последней корректировкой запаса дистанции подхода; затем повторены специализированные проверки навигации и работы.
- `hud_controls`: реальные команды добычи, доставки, отмены, копания, фильтрация каталога, строительство, обучение, пауза и настройки. `rts_render_integration`: движение при пакетной отрисовке, выделение, скрытые противники, наведение на ресурсы и удаление/возврат их экземпляров при смене видимости.

Устранён случайный посторонний символ в GoblinExpeditionSystem, препятствовавший загрузке полной игры. Регрессионная проверка звука обновлена под уже подключённые тематические WAV-записи: старые play_pop/play_hit остаются безопасными совместимыми вызовами; неиспользуемые процедурные звуки не восстановлены. Исходные референсы HUD сохранены без изменения; AtlasTexture выбирает области иллюстраций в нативных Control-элементах.

Оконные запуски на Metal просмотрены: [основной HUD](frontier-hud.png), [категория ресурсов](frontier-hud-resources.png), [нагрузочная сцена](live-battle.png). Игровых ошибок и предупреждений в итоговых журналах нет. Headless macOS выдаёт только прежнее сообщение о системных CA-сертификатах.

### Профиль полной сцены на Apple M1 Pro

Godot 4.7 / Forward+ / Metal, скриншот viewport 3456×2160, 3D масштаб 75%, UI 100%, VSync отключён. `tests/live_performance.tscn`: после 30 кадров прогрева записано 90 кадров каждого этапа. 120 дополнительных бойцов игрока и 64 врага, обычная физика, бой, туман и остальной мир; повышенное здоровье удерживает акторов в сцене. Часть строя ожидает контакта, поэтому 184 добавленных бойца не означают 184 одновременные атаки.

| Этап | Медиана, мс | 95-й процентиль, мс | Всего акторов | Зданий |
| --- | ---: | ---: | ---: | ---: |
| Начальное поселение | 6,163 | 13,182 | 71 | 2 |
| 184 дополнительных бойца, туман | 11,802 | 25,564 | 255 | 4 |
| Полные скелеты, 100% разрешения | 20,298 | 32,568 | 255 | 4 |
| Возврат к сбалансированному режиму | 12,955 | 22,357 | 255 | 4 |
| Расширенное поселение, бой, туман | 14,618 | 29,449 | 271 | 40 |

Сырые данные: [live_performance.json](test-results/2026-09-16/live_performance.json). В предыдущем замере до кэширования локального обхода та же конфигурация нагрузки с четырьмя зданиями давала медиану 156,004 мс и p95 182,962 мс — [сохранённый профиль](test-results/live_performance_before_steering.json). Это разные запуски живой сцены: положение бойцов и фоновые появления мобов меняются.

Короткие замеры показывают устранение основной измеренной нагрузки, но не гарантируют постоянные 60 FPS. Пики времени кадра остаются; продолжительную позднюю кампанию и тысячи одновременно сражающихся акторов этот профиль не покрывает.
